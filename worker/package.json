{
  "name": "@multicom/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm run typecheck && npm test && npm run deploy:dry",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy:dry": "wrangler deploy --dry-run",
    "deploy:production": "wrangler deploy --var TARGET_ORIGIN:https://multicom-storefront-api.multicom-target.workers.dev --var ALLOWED_ORIGINS:https://multicom-web.pages.dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260902.1",
    "typescript": "5.9.2",
    "vitest": "3.2.7",
    "wrangler": "4.128.0"
  }
}
